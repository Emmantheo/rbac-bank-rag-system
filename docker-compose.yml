version: "3.9"

services:
  

  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: accounting_api
    env_file:
      - .env
    ports:
      - "8000:8000"
    restart: unless-stopped

volumes:
  postgres_data: